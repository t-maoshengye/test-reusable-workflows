name: Call a reusable workflow

name: Call reusable workflow example

on:
  push:
    branches:
      - dev

jobs:
  call-workflow:
    uses: t-maoshengye/test-reusable-workflows/.github/workflows/reusable_workflow.yml@dev

  call-workflow-passing-data:
    uses: t-maoshengye/test-reusable-workflows/.github/workflows/reusable_workflow.yml@dev
    with:
      username: ye
